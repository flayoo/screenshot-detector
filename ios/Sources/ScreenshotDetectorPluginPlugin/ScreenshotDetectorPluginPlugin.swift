import Foundation
import Capacitor

/**
 * Please read the Capacitor iOS Plugin Development Guide
 * here: https://capacitorjs.com/docs/plugins/ios
 */
@objc(ScreenshotDetectorPluginPlugin)
public class ScreenshotDetectorPluginPlugin: CAPPlugin, CAPBridgedPlugin {
    public let identifier = "ScreenshotDetectorPluginPlugin"
    public let jsName = "ScreenshotDetectorPlugin"
    public let pluginMethods: [CAPPluginMethod] = [
        CAPPluginMethod(name: "echo", returnType: CAPPluginReturnPromise)
    ]
    private let implementation = ScreenshotDetectorPlugin()

    @objc func echo(_ call: CAPPluginCall) {
        let value = call.getString("value") ?? ""
        call.resolve([
            "value": implementation.echo(value)
        ])
    }
}
